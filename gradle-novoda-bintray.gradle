apply plugin: 'maven'
apply plugin: 'com.novoda.bintray-release'

publish {
    userOrg = Org
    groupId = GROUP
    artifactId = ARTIFACT
    publishVersion = VERSION_NAME
    desc = DESCRIPTION
    website = WEBSITE
}